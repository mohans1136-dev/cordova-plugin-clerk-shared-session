<?xml version="1.0" encoding="UTF-8"?>
<plugin xmlns="http://apache.org/cordova/ns/plugins/1.0"
        xmlns:android="http://schemas.android.com/apk/res/android"
        id="com.outsystems.clerk.sharedsession"
        version="1.0.0">
        
    <name>Clerk Shared Session Sync</name>
    <description>Cordova Plugin for Clerk Shared Session Sync on Android</description>
    <license>MIT</license>

    <!-- Declare the build-time variable requirement -->
    <preference name="CLERK_PUBLISHABLE_KEY" />

    <js-module src="www/clerkPlugin.js" name="ClerkPlugin">
        <clobbers target="cordova.plugins.ClerkPlugin" />
    </js-module>

    <platform name="android">
        <config-file target="AndroidManifest.xml" parent="/*">
            <uses-permission android:name="android.permission.INTERNET" />
        </config-file>

        <config-file target="res/xml/config.xml" parent="/*">
            <feature name="ClerkPlugin">
                <param name="android-package" value="com.outsystems.clerk.sharedsession.ClerkPlugin" />
            </feature>
        </config-file>

        <!-- Inject the variable into Android's strings.xml resource file -->
        <config-file target="res/values/strings.xml" parent="/*">
            <string name="clerk_publishable_key">$CLERK_PUBLISHABLE_KEY</string>
        </config-file>

        <framework src="src/android/build.gradle" custom="true" type="gradleReference" />
        <source-file src="src/android/ClerkPlugin.kt" target-dir="src/com/outsystems/clerk/sharedsession" />
    </platform>
</plugin>